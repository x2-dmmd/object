var name "DmmD"
prt name
jmp section name

if (cmp name == "DmmD")
    prt "Hello, DmmD"
    if (true)
        prt "layered"
    end
else if (cmp name == "Goos")
    prt "Hello Goos"
else
    prt "Hi $(name)"
end

:: Sections
:section arg0
    prt arg0 :: Inline
end

prt "test"